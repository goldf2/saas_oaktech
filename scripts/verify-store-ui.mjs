// Canonical storefront/admin browser regression. The former flat editor was replaced
// by the product-centric workflow; keep the established npm command as its alias.
import "./verify-product-workspace.mjs";
