# 当前状态

## 2026-09-17T05:03:17+08:00 · STORE-01新编辑页已挂载（未上线）

功能工作树已通过实际商品编辑/图片/版本上传/确认发布局部浏览器验证，7组通过，完整88项回归通过。旧商品列表/新建入口和版本保存返回路径尚未整合，任务仍blocked；不是已部署。详细状态见 [商品工作台接续单](PRODUCT_WORKSPACE_HANDOFF.md) 与 [本轮实测证据](evidence/2026-09-17-product-workspace-mounted.json)。原管理员草稿及权限状态不变。

## 2026-09-17T04:28:46+08:00 · STORE-01 商品内统一编辑（部分完成，未上线）

用户要求软件版本归入商品管理，图标/封面/截图/说明与版本在同一工作台编辑，先预览后发布。已建立内部草稿与图片准备服务，9项新测试及完整82项回归通过，类型检查与隔离生产构建通过。新页面/API写入被工具安全检查拦截，未执行；没有新上传HTTP入口或浏览器流程，未推送/部署。

接续见 [PRODUCT_WORKSPACE_HANDOFF.md](PRODUCT_WORKSPACE_HANDOFF.md)；代码保留在`feat/product-workspace-20260917`独立worktree，不能把未接入服务当成用户已能操作。原管理员实现草稿、依赖变更和真实权限均未触碰。

## 2026-09-17T03:57:35+08:00 · 实际超管设置尝试（未完成）

已在用户现有正常浏览器会话中确认登录成功，后台仍提示没有商品管理权限。未读取Cookie/Token，未写入或记录个人subject。当前没有可操作的商城Coolify控制台或已配置的专用权限数据库。

本地新增 `lib/admin/config.mjs`、`schema.mjs`、`backend.mjs` 未接入草稿，`pg`由开发依赖移入运行依赖；只完成Node语法检查，没有把本轮草稿当成通过真实数据库/浏览器验收。随后将服务接入认证、授权和管理页面的工具写入被安全检查拦截，未执行；`lib/auth.ts`、`lib/auth-options.ts`、`lib/store/admin.ts`仍为此前代码。本轮未提交、未推送、未部署，未为真实账号创建超级管理员。现有登录不等于授权成功。

ADM-02标记blocked。断点证据见[2026-09-17-admin-setup-attempt.json](evidence/2026-09-17-admin-setup-attempt.json)；接手须核对实际diff，恢复允许的操作通道后继续审查和验证，不能通过其他执行方式绕过拒绝。


更新时间：2026-09-16T21:14:40+08:00。本轮基线f8b0e8c / 0.1.26，交付候选0.1.27；任务ADM-01。

## 本轮已完成：权限存储与并发工程验证

ADM-01已完成可运行原型，不再只是方案文档。`scripts/admin-poc/`包含隔离PostgreSQL schema、事务原型、多进程测试进程与自动启动/清理脚本。实际PostgreSQL17.10上16个数据库用例通过，外加真实服务器关停/重启持久化检查；20个独立进程并发只有一次初始化成功。

发现并记录两项关键结论：SQL锁无法保护断连后的JSON重命名（PG-14故障反例）；口令有效期须在取得行锁后另发查询重新计算（PG-17在修复前失败、修复后通过）。选择同事务管理命令/目录元数据/授权审计路线，公开下载使用后续待实现的只读投影，不增加匿名下载对角色查询的依赖。详情见[ADR-0001](../adr/ADR-0001-admin-storage.md)。

完整商城回归73/73、类型检查、生产构建通过。[验收证据](evidence/2026-09-16-admin-storage-poc.json)记录了实际环境、失败再修复与未验证范围。GitHub新增真实PostgreSQL service测试job；CI及实际线上版本分别在部署后收据核验。

## 没有完成的部分

**商城仍没有可供真实用户使用的首位超管初始化或后台角色管理。** 当前`lib/store/admin.ts`仍使用环境白名单。本轮原型不被任何生产路由导入，未修改app/lib/components/config/Dockerfile/.env.example，未配置生产数据库/口令、未给实际账号授权。`OAKTECH_BOOTSTRAP_ADMIN_TOKEN`仍不是当前业务可用功能。

- ADM-02及后续：正式schema、安装标识与迁移、正常身份登记、口令寿命与限流、初始化/管理员页面、旧白名单退出及生产真实认领均待实施。
- 生产PG实际地址/私有网络/卷和备份配置仍待ADM-11；本次只定义目标部署边界。
- JSON目录投影的顺序/崩溃恢复/新旧权威源切换须在ADM-09/10完成，不能将PoC事务结果当作整个商城已经迁移。
- 现有商品展示、已发布安装包、Open Play兼容更新保持不变；没有自动发布任何软件Release。

## 接续

唯一下一任务：**ADM-02（身份、角色、安装状态与审计schema）**。从[HANDOFF](HANDOFF.md)和[PROGRESS](PROGRESS.md)接续，任务状态只维护[TASKS.json](TASKS.json)。权限主线目前1/14项完成，这是阶段任务数量，不是页面完成率。

源码提交、CI、实际部署、真实账号授权分别记录。本次部署后收据位于 `.local-verification/0.1.27/`，不能因health变为0.1.27就声称超管已经绑定。

## 2026-09-17T04:38:12+08:00 · STORE-01 继续结果（未完成页面接入）

隔离商品工作树已新增工作台组件、保存/确认发布动作和图片上传/读取路由；新路由/动作6项通过，完整88项、类型及生产构建通过，既有界面14组浏览器回归通过。新商品路由挂载/版本面板的写入被工具安全检查拦截，未执行；新工作台仍不可操作，没有把旧界面测试当新版验收，没有提交、推送或部署。主仓库管理员草稿未被覆盖。最新断点见 [商品工作台交接](PRODUCT_WORKSPACE_HANDOFF.md) 与 [验证范围](evidence/2026-09-17-product-workspace-api-progress.json)。
