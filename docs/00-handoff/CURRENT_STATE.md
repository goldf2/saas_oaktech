# 当前状态

更新时间：2026-09-17T05:47:39+08:00。本轮交付STORE-01，发布候选0.1.28，主线基线8462a3e。

## 商品编辑与发布

商品管理已统一为列表→新建/选择商品→基本资料/图文素材/软件版本/预览发布。新建只保存草稿；已上架商品保存后公开内容不变。版本保存通过兼容入口自动回对应商品，不再手动跳回。图标、封面、截图可上传/排序，预览与公开页面共享渲染；手机预览响应容器宽度。

完整91项测试、包含TypeScript的生产构建，以及12组真实隔离浏览器检查通过；覆盖从真实新建到发布、角色门禁、重复输入、8+1MiB上传、图文/版本共同发布、旧地址兼容、服务重启后持久化与匿名GET/HEAD/Range。证据见 [最终验证](evidence/2026-09-17-product-workspace-complete.json)，操作说明见 [商品工作台](PRODUCT_WORKSPACE_HANDOFF.md)。

本轮未改生产商品数据、未发布真实软件版本、未更改账号权限。提交后真实CI/health/已登录后台只读验收在 `.local-verification/0.1.28/` 单独记录；不能用构建通过代替线上结果。

## 管理员工作与保留边界

ADM-01数据库原型已验证；ADM-02在主工作区留有未提交、未接入的lib/admin草稿和pg依赖调整。本次不会把这些代码提交或部署，旧认证和管理员白名单策略保持原样。用户后来要求本地超管不依赖Casdoor，后续ADM任务须先据此修订设计，不能直接按旧的OIDC先登录认领路线部署。

STORE-01在代码/本地验收维度完成；部署状态另记。下一开发任务ADM-02仍有独立阻塞。任务事实源为 [TASKS.json](TASKS.json)，生成视图 [PROGRESS.md](PROGRESS.md)；没有后台无人值守开发。

版本说明目前中英文仍必填，撤回/删除后可能回商品列表；图片安全清理、跨进程目录事务属于后续专项。完整历史状态保存在archive中，不把历史“路由缺失”当当前状态。
