# 当前状态

## 发布工作流界面 0.1.30（2026-09-17T16:20:34+08:00）

STORE-02实现和本地验收完成，待远程代码交付核验。版本资料→上传文件→校验预览→明确发布已打通，109项单元+14新发布流程+12原工作台浏览器检查、类型及默认Turbopack构建通过。四阶段和详细证据见[发布UI方案](../release-workflow-ui.md)、[验收收据](evidence/2026-09-17-release-workflow-ui.json)。

此变更使用独立worktree feat/release-workflow-ui-20260917，不包括也不覆盖主目录另一会话的本地管理员未提交代码。权限、签名和服务器正式发布规则未改；真实生产商品/软件版本未操作。商品编辑代码部署不等于软件包已正式发布。

公开运行状态以`.local-verification/0.1.30/`后续部署收据为准；不要仅凭提交或health推断真实管理员/软件发布已完成。原权限主线继续按其独立台账状态处理。

## 历史状态

更新时间：2026-09-17T05:47:39+08:00。本轮交付STORE-01，发布候选0.1.28，主线基线8462a3e。

## 0.1.29 · 发布构建兼容修复

更新时间：2026-09-17T05:55:11+08:00。0.1.28主提交8817062已推送，但GitHub运行35154436226的Turbopack构建拒绝`app/admin/software/page.tsx`转导出dynamic；本地Webpack此前通过不代表该CI通过。失败原因已定位，路由改为本文件直接声明`export const dynamic = "force-dynamic"`，共享默认页面保持不变。

修正后主目录的默认`npm run build`已经通过。0.1.29已重新通过91项测试、TypeScript检查、默认Turbopack生产构建和12组新版完整浏览器流程验证；最终脱敏结果另见evidence/2026-09-17-product-workspace-turbopack-fix.json，实际线上版本/页面见.local-verification/0.1.29/deployment.json。未把CI失败写成凭据缺失或忽略。

## 商品编辑与发布

商品管理已统一为列表→新建/选择商品→基本资料/图文素材/软件版本/预览发布。新建只保存草稿；已上架商品保存后公开内容不变。版本保存通过兼容入口自动回对应商品，不再手动跳回。图标、封面、截图可上传/排序，预览与公开页面共享渲染；手机预览响应容器宽度。

完整91项测试、包含TypeScript的生产构建，以及12组真实隔离浏览器检查通过；覆盖从真实新建到发布、角色门禁、重复输入、8+1MiB上传、图文/版本共同发布、旧地址兼容、服务重启后持久化与匿名GET/HEAD/Range。证据见 [最终验证](evidence/2026-09-17-product-workspace-complete.json)，操作说明见 [商品工作台](PRODUCT_WORKSPACE_HANDOFF.md)。

本轮未改生产商品数据、未发布真实软件版本、未更改账号权限。提交后真实CI/health/已登录后台只读验收在 `.local-verification/0.1.28/` 单独记录；不能用构建通过代替线上结果。

## 管理员工作与保留边界

ADM-01数据库原型已验证；ADM-02在主工作区留有未提交、未接入的lib/admin草稿和pg依赖调整。本次不会把这些代码提交或部署，旧认证和管理员白名单策略保持原样。用户后来要求本地超管不依赖Casdoor，后续ADM任务须先据此修订设计，不能直接按旧的OIDC先登录认领路线部署。

STORE-01在代码/本地验收维度完成；部署状态另记。下一开发任务ADM-02仍有独立阻塞。任务事实源为 [TASKS.json](TASKS.json)，生成视图 [PROGRESS.md](PROGRESS.md)；没有后台无人值守开发。

版本说明目前中英文仍必填，撤回/删除后可能回商品列表；图片安全清理、跨进程目录事务属于后续专项。完整历史状态保存在archive中，不把历史“路由缺失”当当前状态。
