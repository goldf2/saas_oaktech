# 2026-09-16 · 并行交接文档独立审校

审校任务：`tsk_88f6f2ca38a4c7db`。审校对象：当前 `saas_oaktech` 仓库；代码基线 `54a42b33bc5fc3e9c42541fdd3bdd6fae4700bce` / `0.1.25`。本文件是审校证据，不是另一套开发方案或任务台账。

## 1. 并行编辑保护与集成约定

2026-09-16T15:51:35+08:00，写入前的工作区检查发现其他会话新增了开发文档。当前审校会话的批量写入在首个断言处终止，**没有覆盖文件，没有创建另一套TASKS或递增版本**。

现有相关主任务为 `tsk_95d98f2ddb1818f0`（商城开发计划与接续机制）与 `tsk_60f6b98e4cfc3b1c`（商城管理员开发方案与接续跟踪）。后续集成应保留一个负责人/提交者统一修改根入口、package版本、进度校验和CI；其他会话仅提交互不重叠的审校资料，避免重复推送和互相覆盖。

## 2. 已独立核实的事实

- 起始Git为main/上述完整SHA，起始工作区干净；本轮检测到的新增文件来自并行编辑，不能当成可清理垃圾或无关错误。
- 本机Node `v24.17.0`、npm `11.13.0`。`.github/workflows/ci-deploy.yml` 使用Node24，`Dockerfile`使用Node22；本文未升级依赖或更改镜像。
- 本轮只读请求公开 `/api/health` 返回HTTP200和 `{"status":"ok","service":"oaktech-software-store","version":"0.1.25"}`。这只是网站代码版本证据，不证明管理员初始化或真实软件包发布。
- `lib/store/admin.ts`仍读取 `OAKTECH_ADMIN_SUBJECTS` / `OAKTECH_ADMIN_USER_IDS` / `OAKTECH_ADMIN_EMAILS`。`lib/auth.ts`验证Casdoor issuer与subject；当前不是持久角色授权体系。
- `lib/store/file-catalog.ts`仅有进程内Promise写队列，且目录不存在时可以返回seed fallback；不能将该机制直接用作权限库或首次认领状态判断。
- 2026-09-16T14:53:58+08:00的本地收尾收据仍报告官网目标软件包未上传/公开及授权会话不足；这是历史证据，继续发布前应重新查询当前版本，不自动发布历史0.6.6.12。
- CT-ADMIN-001 v1.0.0要求事务持久化、原子认领、完成标记永久关闭、即时撤权、最后超管保护、旧名单退出与受控恢复。复制规范或填写拟议变量不能算实现。

## 3. 集成前必须消除的文档分歧

### R1 · 统一任务状态数据源

审校时同时存在：

- `docs/00-handoff/DEVELOPMENT_PLAN.md`：指向 `docs/00-handoff/TASKS.json`，权限任务编号 `ADM-01` 起。
- `docs/admin-bootstrap/DEVELOPMENT_PLAN.md`：指向 `docs/admin-bootstrap/tasks.json`，权限任务编号 `ADM-001` 起。

两处都宣称自己的任务文件是唯一源，不能同时作为同一工作流的事实来源。应由主集成者选定一个规范台账，另一处改为专题附录/引用或明确归档。进度页从同一台账生成，验收编号和交接入口同步对齐；不要各自更新状态造成互相矛盾。

### R2 · 数据库选型不得被并行文档意外定稿

审校时总方案优先评估PostgreSQL，管理员专题优先验证单机SQLite/better-sqlite3；两者均标为提案，没有现有代码或用户确认支持“已经选择并部署”。

当前可以保留两种候选的权衡，但必须由同一个存储选型任务/ADR给出最终决定，并据此统一模型、迁移、Docker验证、锁策略和恢复命令。不要同时开发两套适配器，也不要在最终汇报中把其中一种说成已配置。权限库可以并发不代表JSON业务目录支持多副本。

### R3 · 单一提交者与版本一致性

多个会话不能同时各自把0.1.25改成0.1.26并混合暂存文件。主集成者应审阅全部计划内变更，明确纳入范围，核对package与lockfile的所有根版本，再进行一次统一验证、提交、推送。其他会话不得抢先强推、重置或自动暂存整个仓库。

### R4 · 可移植证据与完成语义

`.local-verification/`及另一台机器的绝对路径不能成为其他人员唯一的证据。仓库应保留脱敏摘要、真实起始提交、命令/结果/时间与尚未验收项。历史63项测试不是新增超管测试；本次文档任务完成与超管功能完成分开。

## 4. 独立审校的交付门槛

主集成完成后应再验证：根入口与链接可解析；仅一个权限任务状态源；任务ID/依赖与验收映射无冲突；生成进度页与JSON同步；原始标准快照保持一致；计划内提案明确未实现；本轮校验/单测/类型/构建与push/CI/部署分别记录。

本审校不改生产身份、授权名单、商品目录、软件包或现有主文档。未执行的测试和发布步骤不记录为通过。最终消歧/复验结果追加在本文件后部，保留上述观察时间与原始发现。

## 5. 2026-09-16T15:58:00+08:00 · 消歧与独立验证

- R1/R2已在活动文档中收敛：另一会话将自己的`docs/admin-bootstrap/`两份草案私下归档，活动目录不再存在。新增`ADMIN_PLAN_REVIEW.md`明确引用唯一主方案`docs/00-handoff/DEVELOPMENT_PLAN.md`、唯一台账`TASKS.json`与A01～A40测试编号；数据库仍为待ADM-01验证的PG提案，SQLite只是备选，不存在已部署选型事实。
- 主台账当前21项任务，其中ADM-01～ADM-14共14项权限主线；实际业务尚未实现。PLAN-01的文档完成不能计入权限业务任务完成数。
- 独立只读执行校验函数：11项正常/负向检查全部通过，包括重复ID、缺失依赖、依赖循环、无负责人、无完成证据、伪报生产完成、规范快照漂移、路径越界、缺失证据和确定性生成。测试仅修改内存副本，未改变实际TASKS或代码。
- 详细结果、校验器SHA-256及观察时间见[独立校验记录](2026-09-16-independent-validator-check.json)。
- 已独立运行`npm run handoff:check`，退出码0；当前根入口、接续单、运行手册和生成进度页存在且通过该脚本检查。本结论不涵盖尚未执行的本轮完整类型检查、构建、CI或生产部署。

### R5 · 建议补齐已完成状态与实现字段的一致性检查

对校验器初版的纯内存负向探测发现：给PLAN-01与ADM-01填入通过的证据元信息、负责人和完成时间，设为done且满足依赖后，ADM-01仍保留`implementation=not_started`也能通过结构校验。实际台账没有这样伪标，本项仅指出校验规则可加强之处。

建议主集成者定义implementation/deployment允许值与done的组合约束，补回归测试：业务feature不能同时done和not_started；documentation仍允许documentation_only；未完成ADM-14不能宣称生产已验收。不能用脚本检查替代对证据内容和真实执行结果的审查。

本审校新增文件范围仅本报告与独立校验JSON；不抢改主脚本、根文档、版本或CI，不另行触发生产部署。主集成者可在统一PLAN-01提交中纳入两份脱敏审校证据。
