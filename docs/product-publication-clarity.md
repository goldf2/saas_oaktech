> 当前候选0.1.37：视频标题已改为可选，省略/空字符串/纯空白均允许，页面使用本地默认显示名称；不请求第三方标题，也不下载原视频。无标题不再是商品发布缺项。

## 0.1.37 更新

在0.1.36既有双栏重排和页签定位基础上，视频标题及封面改为可选；有效链接仍必填，空备用来源仍需补齐或移除。默认显示“视频介绍”/“Video introduction”，不是自动获取平台标题。保存、预览和确认发布仍独立，软件不受影响。用户真实截图确认旧标题校验阻止商品发布；本次不自动公开正式草稿。

# 商品资料发布澄清（PUB-02）

用户反馈OpenPlay新介绍和视频未显示在浏览页。本轮普通公开浏览器分别核实中文/英文详情页均没有视频、仍为旧介绍；读取用户已有登录态脚本被安全检查拒绝，桌面原生观察因辅助权限不可用失败，因此尚不能确定生产草稿停在哪个阶段。不能把推测写成真实未点击发布或数据丢失。

修正已确认的UX问题：保存按钮名为“保存草稿”；独立“发布商品资料”准备按钮先保存当前输入，再显示确认，准备动作绝不发布。状态同时标明线上旧资料和本次待发布。确认显示具体字段、中文说明前后对照、视频计数和标题，中文变更/英文未改给出提示。视频空标题/空来源可直接定位；成功后有双语公开页链接。原纯预览不保存，软件独立发布不动商品，认证和存储未放宽。

不迁移、删改或公开用户生产数据；部署新界面不等于OpenPlay资料已发布。

## 用户补充截图与整页重排

用户随后提供的真实截图显示：视频链接已识别、视频标题为空，页面提示商品资料有待发布修改。已读取的服务校验要求视频标题和来源齐全，因此空标题是该画面下可证实的发布阻塞；未读取生产审计，不能断言用户曾完成哪一次发布请求。

新版商品编辑保留三组和两条独立发布动作，重新安排页面：

- 顶部将“线上介绍”和“本次资料”明确分开，软件发行数量只在软件版本分区说明。
- 一条固定工具栏包含分区导航、保存草稿、纯预览和准备商品发布。资料末尾保留相同操作，避免长页面必须返回页首。
- 桌面商品资料为两栏：左侧名称、类别、说明与可选英文；右侧图标、封面和缩略图列表。手机按阅读顺序排列，不能全页缩放压小文字。
- 视频去除多层大边框，以链接为首要字段，标题必填提示就地显示；可选封面放侧栏，自定义来源名称折叠，备用来源仍可直接添加和排序。
- `ProductDetailsEditor`只接收受控值和既有动作回调，不拥有新的持久化逻辑；切换页签、预览、语言时不丢未保存内容。

新增 `scripts/verify-product-editor-layout.mjs` 在临时存储、回环服务和合成测试会话中复现截图：空标题阻断、点击定位、保存与确认分离、无安装包发布商品、公开中英文视频、纯预览不保存、真实素材上传、320/390/1024/1440宽度。执行结果单独落在 `.local-verification/product-layout/`，不得沿用改版前测试数量。

本轮有一次包含发布核对区再压缩和全量单元测试的命令被工具安全检查拒绝，未执行，也未以其他入口重做该操作。已成功的资料/视频整页重排可继续独立构建和专项验证；未完成的全量验证必须在交付说明中保留。

## 2026-09-19T06:47:05+08:00 · 并行工作隔离

原树在本轮操作中出现并行提交877c704和依赖重装，浅色专项复核曾报缺少next/dist/compiled/zod，不能归因于业务代码。已把5项重排文件移入独立编辑树并保留原树。新树从新检查点开始集成验收，不重做此前被拒的发布核对区再压缩操作；后续测试结果另记。
